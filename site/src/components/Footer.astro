---
---

<footer>
  <p>Looking for my next high-ownership backend role — Staff Engineer, Founding Engineer, or Backend Architect.</p>
  <a href="mailto:mail4udhaya@gmail.com">mail4udhaya@gmail.com</a>
</footer>

<style>
  footer {
    border-top: 1px solid var(--rule);
    padding: var(--space-10) 0;
    text-align: center;
    margin-top: var(--space-10);
  }

  footer a {
    font-family: "IBM Plex Mono", monospace;
    font-size: var(--fs-16);
    font-weight: 600;
    color: var(--accent);
    text-decoration: none;
    border-bottom: 2px solid transparent;
    transition: border-color 0.2s ease;
    display: inline-block;
    margin: var(--space-2) 0;
  }

  footer a:hover,
  footer a:focus {
    border-bottom-color: var(--accent);
  }

  footer a:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 4px;
  }

  footer p {
    font-size: var(--fs-14);
    color: var(--ink-2);
    margin-bottom: var(--space-4);
  }
</style>
