---
interface Props {
  tools: string[];
}

const { tools } = Astro.props;
---

<div class="tool-grid reveal">
  {tools.map((tool) => (
    <div class="tool-card">
      <div class="tool-name">{tool}</div>
    </div>
  ))}
</div>

